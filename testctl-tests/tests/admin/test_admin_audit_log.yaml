test_case: admin_audit_log_accessible
module: admin
auth:
  role: admin

steps:
  - action: GET
    path: /admin/audit-log
    expect_status: 200
    text_contains: "Audit"   # page heading is "Audit Log"
