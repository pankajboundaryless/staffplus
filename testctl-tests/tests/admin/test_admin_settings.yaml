test_case: admin_settings_save
module: admin
auth:
  role: admin

steps:
  - action: GET
    path: /admin/settings
    expect_status: 200

  - action: POST
    path: /admin/settings
    body:
      company_name: "Testctl Company"
    expect_status: 200   # app responds 200+HTML, not PRG redirect
