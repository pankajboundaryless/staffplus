test_case: reports_wip_accessible
module: reports
auth:
  role: admin

steps:
  - action: GET
    path: /reports/wip
    expect_status: 200
    validations:
      - type: text_contains
        value: "Work in Progress"
