test_case: invoicing_cross_charge_form_accessible
module: invoicing
auth:
  role: admin

steps:
  - action: GET
    path: /invoicing/cross-charge/new
    expect_status: 200
    text_contains: "Cross-Charge"   # page heading: "New Cross-Charge Invoice"
