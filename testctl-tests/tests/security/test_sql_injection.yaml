test_case: security_sql_injection_in_search
module: security
auth:
  role: admin

steps:
  - action: GET
    path: "/people?search=1%27+OR+%271%27%3D%271"
    expect_status: 200
    text_contains: "People"   # page must load normally, not crash
