test_case: security_no_auth_bypass_on_post
module: security

steps:
  - action: POST
    path: /time/store
    body:
      project_id: "14"
      hours:      "8"
    follow_redirect: false
    expect_status: 302   # unauthenticated POST must redirect to login, not store data
