test_case: people_list_accessible
module: people
auth:
  role: admin

steps:
  - action: GET
    path: /people
    expect_status: 200
    text_contains: "People"   # page heading — present regardless of list being empty
