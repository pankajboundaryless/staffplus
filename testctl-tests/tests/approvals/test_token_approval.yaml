test_case: approvals_token_deeplink_respond
module: approvals
skip: true
skip_reason: "Token-based approval deeplink (/approvals/respond?token=...) not yet implemented — returns 404"
auth:
  role: admin

steps:
  - action: GET
    path: /approvals/respond?token=testctl-step-token-xyz789&action=approve
    expect_status: 302
